export interface LoginProps {
    username: string;
    password: string
    providerName: string;
    providerIcon: string;
}

export interface CustomFile {
    name: string;
    size: number;
    type: string;
}
  